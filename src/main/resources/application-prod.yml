server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:history_museum}?useSSL=${SSL:false}&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root123}

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  web:
    resources:
      static-locations: classpath:/static/

# --- LLM 代理配置 ---
llm:
  base-url: ${LLM_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
  api-key: ${LLM_API_KEY:}
  model: ${LLM_MODEL:glm-4-flash}

# --- Embedding 配置 ---
embedding:
  provider: ${EMBEDDING_PROVIDER:zhipu}
  base-url: ${EMBEDDING_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
  api-key: ${EMBEDDING_API_KEY:}
  model: ${EMBEDDING_MODEL:embedding-3}
  dimensions: ${EMBEDDING_DIMENSIONS:1024}

logging:
  level:
    com.history: INFO
